# Dependencies
node_modules/

# Runtime databases (each app seeds/creates its own on first run)
data/*.db
data/*.db-shm
data/*.db-wal
data/submissions.json

# Environment / secrets
.env
.env.local
.env.*.local

# OS / editor files
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Build artifacts (none currently exist, but reserved)
dist/
build/
